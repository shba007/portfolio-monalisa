name: 'portfolio-monalisa-dev'
services:
  app:
    # image: 'ghcr.io/Algostract/portfolio-monalisa:latest'
    restart: on-failure:3
    build: .
    volumes:
      - type: bind
        source: ./static
        target: /app/static
    env_file:
      - .env.prod
    ports:
      - 4200:3000
