name: 'portfolio-monalisa'
services:
  app:
    image: 'ghcr.io/Algostract/portfolio-monalisa:latest'
    restart: on-failure:3
    volumes:
      - type: bind
        source: ./static
        target: /app/static
    env_file:
      - .env.prod
    ports:
      - 4200:3000
